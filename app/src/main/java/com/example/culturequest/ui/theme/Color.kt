package com.example.culturequest.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color definitions for the CultureQuest application.
 *
 * This file contains all Material 3 color values used by the app, separated
 * into light and dark theme variants. These colors are referenced by the
 * theme configuration to ensure consistent visual styling across the UI.
 *
 * All color values follow the Material Design color system and are defined
 * using ARGB hexadecimal notation.
 */

// ---------------------------- Light Theme Colors ----------------------------

/**
 * Primary color used for prominent UI elements in light theme.
 */
val md_theme_light_primary = Color(0xFF386A1F)

/**
 * Color applied to content displayed on top of the primary color in light theme.
 */
val md_theme_light_onPrimary = Color(0xFFFFFFFF)

/**
 * Container color for primary elements in light theme.
 */
val md_theme_light_primaryContainer = Color(0xFFB7F396)

/**
 * Color used for content on primary containers in light theme.
 */
val md_theme_light_onPrimaryContainer = Color(0xFF052100)

/**
 * Secondary color used for less prominent UI elements in light theme.
 */
val md_theme_light_secondary = Color(0xFF4D6042)

/**
 * Color applied to content displayed on top of the secondary color in light theme.
 */
val md_theme_light_onSecondary = Color(0xFFFFFFFF)

/**
 * Container color for secondary elements in light theme.
 */
val md_theme_light_secondaryContainer = Color(0xFFD9E7CB)

/**
 * Color used for content on secondary containers in light theme.
 */
val md_theme_light_onSecondaryContainer = Color(0xFF131F0D)

/**
 * Tertiary color used for decorative or accent UI elements in light theme.
 */
val md_theme_light_tertiary = Color(0xFF386668)

/**
 * Color applied to content displayed on top of the tertiary color in light theme.
 */
val md_theme_light_onTertiary = Color(0xFFFFFFFF)

/**
 * Container color for tertiary elements in light theme.
 */
val md_theme_light_tertiaryContainer = Color(0xFFBBEBED)

/**
 * Color used for content on tertiary containers in light theme.
 */
val md_theme_light_onTertiaryContainer = Color(0xFF002021)

/**
 * Color used to represent errors in light theme.
 */
val md_theme_light_error = Color(0xFFBA1A1A)

/**
 * Color applied to content displayed on top of error surfaces in light theme.
 */
val md_theme_light_onError = Color(0xFFFFFFFF)

/**
 * Background color for screens in light theme.
 */
val md_theme_light_background = Color(0xFFFDFDF5)

/**
 * Color used for text and icons displayed on the background in light theme.
 */
val md_theme_light_onBackground = Color(0xFF1A1C18)

/**
 * Default surface color used for cards and sheets in light theme.
 */
val md_theme_light_surface = Color(0xFFFDFDF5)

/**
 * Color used for content displayed on surfaces in light theme.
 */
val md_theme_light_onSurface = Color(0xFF1A1C18)

// ---------------------------- Dark Theme Colors -----------------------------

/**
 * Primary color used for prominent UI elements in dark theme.
 */
val md_theme_dark_primary = Color(0xFF9CDF7D)

/**
 * Color applied to content displayed on top of the primary color in dark theme.
 */
val md_theme_dark_onPrimary = Color(0xFF163900)

/**
 * Container color for primary elements in dark theme.
 */
val md_theme_dark_primaryContainer = Color(0xFF245106)

/**
 * Color used for content on primary containers in dark theme.
 */
val md_theme_dark_onPrimaryContainer = Color(0xFFB7F396)

/**
 * Secondary color used for less prominent UI elements in dark theme.
 */
val md_theme_dark_secondary = Color(0xFFBDCBAF)

/**
 * Color applied to content displayed on top of the secondary color in dark theme.
 */
val md_theme_dark_onSecondary = Color(0xFF283420)

/**
 * Container color for secondary elements in dark theme.
 */
val md_theme_dark_secondaryContainer = Color(0xFF3E4A35)

/**
 * Color used for content on secondary containers in dark theme.
 */
val md_theme_dark_onSecondaryContainer = Color(0xFFD9E7CB)

/**
 * Tertiary color used for decorative or accent UI elements in dark theme.
 */
val md_theme_dark_tertiary = Color(0xFF9FCFCC)

/**
 * Color applied to content displayed on top of the tertiary color in dark theme.
 */
val md_theme_dark_onTertiary = Color(0xFF003739)

/**
 * Container color for tertiary elements in dark theme.
 */
val md_theme_dark_tertiaryContainer = Color(0xFF1E4E50)

/**
 * Color used for content on tertiary containers in dark theme.
 */
val md_theme_dark_onTertiaryContainer = Color(0xFFBBEBED)

/**
 * Color used to represent errors in dark theme.
 */
val md_theme_dark_error = Color(0xFFFFB4AB)

/**
 * Color applied to content displayed on top of error surfaces in dark theme.
 */
val md_theme_dark_onError = Color(0xFF690005)

/**
 * Background color for screens in dark theme.
 */
val md_theme_dark_background = Color(0xFF1A1C18)

/**
 * Color used for text and icons displayed on the background in dark theme.
 */
val md_theme_dark_onBackground = Color(0xFFE3E3DC)

/**
 * Default surface color used for cards and sheets in dark theme.
 */
val md_theme_dark_surface = Color(0xFF1A1C18)

/**
 * Color used for content displayed on surfaces in dark theme.
 */
val md_theme_dark_onSurface = Color(0xFFE3E3DC)
